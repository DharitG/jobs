# Environment variables for JobBright Backend (local development)

# Database URL for accessing the service running in Docker from the host machine
DATABASE_URL=postgresql://user:password@localhost:5432/db

# Security
SECRET_KEY=a_very_secret_key_that_should_be_changed

# Auth0 (Required for production/deployment)
AUTH0_DOMAIN=your-auth0-tenant.us.auth0.com
AUTH0_API_AUDIENCE=https://api.opencrew.io

# Celery/Redis (assuming default ports exposed on host)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Qdrant (Required if matching features are enabled)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Stripe (Required if payment features are enabled)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_... # Note: Publishable key often only needed by frontend
STRIPE_WEBHOOK_SECRET=whsec_... # Get this from Stripe dashboard after creating webhook endpoint

# OpenAI (Required if AI features like resume editing are enabled)
# OPENAI_API_KEY=sk-...
