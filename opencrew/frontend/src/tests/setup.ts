import { expect } from 'vitest';
// Extend Vitest's expect functionality with testing-library matchers
import '@testing-library/jest-dom/vitest'; // Use the vitest extension point

// Add any other global test setup here, e.g.:
// - Mocking global objects (fetch, localStorage)
// - Setting up MSW (Mock Service Worker)

console.log('Vitest setup file loaded.');
